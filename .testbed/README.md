# Vendor Adapter Testbed

This hidden testbed owns vendor adapter smoke tests, fixtures, demos, browser scenes where useful, and generated local dependency symlinks.

Do not commit `.testbed/node_modules`. Vendor DTO fixtures may live under `fixtures/` or `.testbed/debug-data/` when they are needed for local validation.
