// @ts-check

/**
 * Demo root used by concrete vendor repos as a starting point.
 *
 * @type {HTMLElement | null}
 */
const app = document.querySelector("#app");

if (app instanceof HTMLElement) {
  app.textContent = "Replace this demo with vendor adapter inspection UI.";
}
